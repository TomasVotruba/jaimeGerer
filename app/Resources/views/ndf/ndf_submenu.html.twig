{% block submenu %}
	<ul class="nav nav-tabs">
		<li {% if app.request.pathInfo == "/ndf/recu/liste"  %} class="active" {% endif %}>
        	<a href="{{ path('ndf_recus_liste') }}" >Mes reçus</a>
      	</li>
      	<li {% if app.request.pathInfo == "/ndf/liste"  %} class="active" {% endif %}>
        	<a href="{{ path('ndf_liste') }}" >Mes notes de frais</a>
      	</li>
	</ul>
{% endblock %}